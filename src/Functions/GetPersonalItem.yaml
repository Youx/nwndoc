---
type: Function
name: GetPersonalItem
shortdesc: Retrieves the string that is the identifier of the personal item for the assassin.
return: string
parameters:
description: |
  Returns the string set by the function SetPersonalItem that represents the identifier of a personal item for an assassin. 
  This item can later be used to prove that the PC have dispatched the assassin.
remarks: |
  This function is of little use for general scripting needs, however is a good example on how to script rewards.
requirements: |
  #include "nw_j_assassin"
version: 1.22
seealso:
  functions:
    - CreatePersonalItemOn
    - GivePlayerPersonalItem
    - PlayerHasPersonalItem
    - SetPersonalItem
  categories:
    - Module Specific
author: Tom Cassiotis

