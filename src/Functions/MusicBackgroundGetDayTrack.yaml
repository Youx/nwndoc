---
type: Function
name: MusicBackgroundGetDayTrack
shortdesc: Gets the daytime background track for an area.
return: int
parameters:
  - type: object
    name: oArea
    desc: | The area to return the background music track of.

description: |
  This function returns an integer value that is associated with the currently set daytime background track.
remarks: |
  There are no constants for the returned track number; instead please refer to Resources > Sounds & Music > Music for track numbers.
version: 1.29
example: |
  object oArea = GetArea(OBJECT_SELF);
  int iBattleTrack = MusicBackgroundGetDayTrack (oArea);
seealso:
  categories:
    - Area
    - Music Effects
author: Michael Nork
editor: Charles Feduke
additional_contributors:
  - Tyson McCann
