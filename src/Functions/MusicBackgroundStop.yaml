---
type: Function
name: MusicBackgroundStop
shortdesc: Stops playing the currently selected background track.
return: void
parameters:
  - type: object
    name: oArea
    desc: | The area to affect.

description: |
  This function terminates playing the background track.
version: 1.22
example: |
  object oArea = GetArea(OBJECT_SELF);
  MusicBackgroundStop(oArea);
seealso:
  categories:
    - Area
    - Music Effects
author: Michael Nork
editor: Jeff Lindsey

