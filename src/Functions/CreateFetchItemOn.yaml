---
type: Function
name: CreateFetchItemOn
shortdesc: Doesn't do anything.
return: void
parameters:
  - type: object
    name: oPC
description: |
  From the source code: 
  // CreateItemOnObject(GetFetchItem(),oPC); 
  // * December 13 2001 
  // * This function no longer does anything 
  // * we changed this so the NPC starts with the 
  // * item on him 
remarks: |
  Found in: nw_j_fetch.nss: 199 
  Indeed, this function doesn’t do anything – there’s nothing in the function!
requirements: |
  #include "nw_j_fetch"
version: 1.28
seealso:
  functions:
    - GetFetchee
  categories:
    - Prototyped but Unused
author: Lilac Soul
additional_contributors:
  - Lilac Soul
