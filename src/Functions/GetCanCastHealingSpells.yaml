---
type: Function
name: GetCanCastHealingSpells
shortdesc: Gets a TRUE or FALSE depending on if the target can cast Healing spells or not.
return: int
parameters:
  - type: object
    name: oTarget
    desc: | The object you want to examine.

description: |
  Returns TRUE if GetCreatureTalentBest(TALENT_CATEGORY_BENEFICIAL_HEALING_TOUCH, 20, oTarget) returns a Valid Talent.
remarks: |
  Function found in nw_i0_plot.nss on line 130.
requirements: |
  #include "nw_i0_plot"
version: 1.22
seealso:
  categories:
    - Talents/Skills/Feats
  constants:
    - TALENT_CATEGORY_* Constants
author: John Shuell

