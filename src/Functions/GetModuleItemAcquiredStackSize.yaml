---
type: Function
name: GetModuleItemAcquiredStackSize
shortdesc: Determines the stack size of an item belonging to a module that was just acquired.
return: int
parameters:
description: |
  This function works only in scripts assigned to the OnItemAcquired event. Returns the size of the stack of the item that was just acquired.
version: 1.30
seealso:
  functions:
    - GetItemStackSize
    - GetModuleItemAcquiredBy
  categories:
    - Inventory
    - Module
  events:
    - OnAcquireItem
author: Charles Feduke

